pip-system-certs>=0
Flask>=3.0,<4.0
Flask-WTF>=1.2,<2.0
# The production server (KEYPARAMS_ENV=production) — pure Python, no C
# extensions to build, and unlike gunicorn it runs on Windows.
waitress>=3.0,<4.0
easyocr>=1.7,<2.0
# Windows' own recognition engine: hundreds of times faster than EasyOCR on
# the scans this program reads. Windows-only, hence the marker; without it the
# app falls back to EasyOCR.
winocr>=0.0.15; sys_platform == "win32"
Pillow>=10.0,<13.0
openpyxl>=3.1,<4.0
pdfplumber>=0.11,<0.12
reportlab>=4.0,<5.0
anthropic>=0.70,<1.0
natasha>=1.6,<2.0
